[project]
name = "mcp-storage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy",
    "pymysql",
    "fastmcp",
]

[dependency-groups]
dev = [
    "click>=8.2.1",
    "ruff>=0.12.8",
]
